{
    backend => 'test://localhost/yancy.data.json',
    collections => {
        people => {
            'x-list-columns' => [ 'name', 'email' ],
            required => [ 'name', 'email' ],
            example => {
                name => 'John Doe',
                email => 'john@example.com',
            },
            properties => {
                name => {
                    type => 'string',
                },
                email => {
                    type => 'string',
                    pattern => '^[^@]+@[^@]+$',
                },
            },
        },
    },
}
